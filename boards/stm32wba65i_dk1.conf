# STM32WBA65RI: 512 KiB SRAM - plenty of room for the Berry heap
CONFIG_COMMON_LIBC_MALLOC_ARENA_SIZE=196608

# temp(): die temperature sensor (via ADC4)
CONFIG_SENSOR=y

# joy(): 5-way joystick on ADC4 channel 6 (adc-keys, input subsystem)
CONFIG_INPUT=y
